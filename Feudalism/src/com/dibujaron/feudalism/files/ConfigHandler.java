package com.dibujaron.feudalism.files;

import org.bukkit.configuration.file.FileConfiguration;

public class ConfigHandler {
	//TODO public static variables for various configurable values
	//used like if(ConfigHandler.battlesAllowed) from other classes
	
	public static void loadConfig(FileConfiguration cfg){
		//This method is called from onEnable and is passed getConfig()
		//loads the values from the config and stores them in static class variables
	}
}
